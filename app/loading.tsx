export default function Loading() {
  return (
    <div className="page-wrap">
      <section className="panel p-8 sm:p-10">
        <span className="kicker">Loading</span>
        <h1 className="mt-5 text-3xl font-semibold sm:text-4xl">Preparing the page…</h1>
        <p className="muted mt-4 max-w-2xl">This should only take a moment.</p>
      </section>
    </div>
  );
}

